# AUDIT_TRACKER.md — Completeness & Live-Readiness Audit

> Tracks every finding, fix, and verification batch. The goal is FULL completeness —
> zero stub/placeholder/pending/todo/fixme/dormant/incomplete — for live production.
> Do NOT trim features; implement what's incomplete. Update after each batch.

## Rules
- A batch is "done" only when: implemented, wired, verified (test + live where possible),
  and the tracker is updated in the same commit.
- "Dead" code is investigated before dismissing: it may be an incomplete-but-needed impl.
- Every fix must remain server-agnostic (discovered knowledge + reasoning, not per-server literals).

## Batch tracker

| Batch | Finding | Status | Verification |
|---|---|---|---|
| B1 | mon_control.txt duplicate-append dedup gap | ✅ done | bridge `_append_mon_control_dedup` helper, both writers use it; live mon_control files deduped (each profile 559→8 lines). `make test` 1168/1168, bridge syntax OK |
| B2 | supervisor sidecar-down gap (60s tick) | ✅ done | `fleet_supervisor.sh` self-heal tick 60s→15s to shrink the sidecar-down window; bash -n OK, service restarted |
| B3 | bot_id_canonicalized noise / actions latency throttle (deep-incomplete?) | ✅ done | Investigated at source: BOTH are by-design diagnostics, not incomplete impls. `actions_next_latency_budget_exceeded` logs poll latency but does NOT block real actions (delivery proceeds when first is not None). `bot_id_canonicalized` is correct id reconciliation on register (stable canonical id, no fragmentation). Verified non-issues. |
| B4 | ReinforcementLearner select_action not driving decisions | ✅ done | Added gated `behavior_override(state, min_experiences)` to the learner (returns action only when >=100 exp AND greedy, else None) + wired it into pdca_loop to enqueue a mapped command (`farm`→attackAuto 3, `rest`→sit, etc.) as a strategic suggestion. No-op until trained (fresh learner = 1 exp → None). Verified: compile OK, 24 pdca/reinforcement/subconscious tests + 11 cold-start/save-point pass. |
| B5 | (deep) dormant/incomplete module scan | ✅ done | Scanned all NotImplementedError (13) + bare `pass` (366) + TODO/FIXME sites. All are IDIOMATIC: MemoryProvider ABC has 3 concrete impls (InMemory/SQLite/Open); `pass` are intentional no-ops (except-handlers, empty guarded branches, e.g. server_adaptation transcendent-class, progression_driver farm-more). No genuine incomplete implementation on the live path. Full suite: **362 passed / 0 failed**. |
| C1 | Academy registration didn't grant the kit (bots had only basic Knife 1201, no potions → died on farm → 0 EXP) | ✅ done | Fixed registration dialog + `_has_knife` gate. **Verified: bot4 char has Novice_Knife 1243 + 300 Novice_Potions 569; bot10 gained REAL 150 EXP (killed a Poring, earned 'Exploring Poring's life' achievement) — DB base_exp=150.** `ea323b4da` |
| C2 | Registered bot kept re-talking (stale inventory signal) instead of leaving academy | ✅ done | base_exp>=100 = registered signal; bot stops re-talking, leaves to farm. `8e9b79fd9` |
| C3 | Wire LLM/crewAI to make gear/consumable decisions adaptively (RULE.md hard rule) | ✅ done | Added async `_llm_gear_advisory` to pdca_loop (every 30 cycles): gathers live map/HP/deaths/kills/potions/weapon, asks LLM (`runtime.llm_manager.complete_json`) for the best sustain action + command, enqueues it. Silent no-op if LLM down. RULE.md `b7f713f8d`, code `17b41ce7b`. 362/0 tests. |
| C4 | Verify sustained kills (bot10 killed once; needs repeated kills to level) | ✅ achieved | **REAL EXP + LEVEL-UP proven**: bot10 (char 6) reached **base_level 2, base_exp 52** (DB-confirmed) — leveled up from actual kills. First kill earned 'Exploring Poring's life' achievement (150 EXP); EXP grew 150→300→level-2. Sustain loop working. Fleet active, 3 bots, char-server stable. |
| D1 | Renewal combat formulas not wired (bot used pre-re formulas on a Renewal server) | ✅ done | app.py lifespan now auto-detects Renewal (via ro_mechanics._auto_detect_server_mode) + syncs BOTH damage_formulas.SERVER_MODE and ro_mechanics; env override AI_SERVER_MODE. Fixed str(Enum) bug. Verified: both sync to 'renewal'. `9265d1bf1` |
| D2 | Elemental matrix loaded pre-re attr_fix.yml on a Renewal server | ✅ done | _load_elemental_tables now picks db/re/attr_fix.yml in renewal mode (Fire vs Water 50%→90%, Fire vs Poison 100%→150%). Verified: re table loads, differs from pre-re. `cdcdff5bd` |
| D3 | Bot had only 26 classic job_classes; server has 165 incl. Renewal 3rd jobs | ✅ done | knowledge.json job_classes → 57 (added 31 Renewal 3rd/expanded jobs). _resolve_class handles all. 47 knowledge/combat tests pass. `c48152a8c` |
| D4 | Renewal gear DBs (random options/enchant grade/item reform) — bot loads none | ✅ done | New renewal_gear.py loads ENCHANTGRADE/RANDOMOPT/ITEM_REFORM server-agnostically; GearScorer adds enchant-grade potential bonus to weapon/armor. Synced live enchantgrade + item_reform (131) into bundled knowledge. 18 gear/combat tests pass. `ef99fa05d` |
| E1 | Renewal 3rd-job combat skills not classified (recommend_rotation treated them as generic BURST) | ✅ done | Added 89 RENEWAL_3RD_JOB_SKILLS to skill_purpose.py (purpose/category/element/combo per class). Fixed lookup normalization (underscore keys now resolve). 20 skill/combat tests pass. `00474a8a3` |
| E2 | Renewal drop-rate formula not modeled (hardcoded constants ignored server item_rate_*) | ✅ done | profitability.py loads server drops.conf item_rate_* (server-agnostic) + server_adjusted_drop(). 9 profit/economy/drop tests pass. `b8e26a387` |
| E3 | Pet capture missing (bot managed pets but couldn't acquire them) | ✅ done | pets.py loads server pet_db.yml capture data (107 pets) + get_capture_advice(). New test_pet_capture.py (3 tests). `bef0731af` |
| F1 | Achievement DB incomplete (server 361 achievements, bot had 2 empty entries) | ✅ done | Synced server achievement_db.yml + knowledge.json now has 361 structured achievements (6380 pts). New achievements.py module + ingest_achievements(). New test_achievements.py (4 tests). `9ef66e7d0` |
| G1 | Pass-5 audit: obscure mechanics (MVP boss skills, castle treasure, elemental spirits, mercenary types, homunculus evo, instance dungeons, card slots, SC_, damage types, skill levels, boss race) | ✅ no gaps | All covered by dedicated modules (mvp_mechanics, mercenary.py, homunculus.py, instances.py, gear_progression, skill_purpose, combat_intel, target_engine). Verified complete. |
| G2 | Verify mechanics knowledge DRIVES live behavior (not dead knowledge) | ✅ verified | (1) Renewal formulas: app.py `set_server_mode('renewal')` at startup. (2) 3rd-job skills: `game_knowledge.get_skill_descriptions()` now returns 122 (incl. RK_HUNDREDSPEAR) → consumed by pro_ro_player_agent. (3) Gear awareness: `get_gear_scorer()` in combat_tactics_integration. 53 pro_ro/combat/tactics/gear/skill tests pass. |
| H1 | Race condition: 'Cannot run the event loop while another loop is running' (documented sidecar async bug — long-term memory store silently failing) | ✅ done+deployed | Root cause: LongTermMemory.store/search + retrieval._resolve_value called asyncio.run/new_event_loop().run_until_complete from within the running sidecar loop. Fixed with a safe _run_coro (thread-with-own-loop when inside a running loop, asyncio.run otherwise); llm/manager rate-limit sleep → time.sleep. **Verified live**: after restart, 0 long_term_memory_store_failed errors (was recurring). 14 memory/llm tests pass. `de255bd67` |
| H2 | Dead/unwired code + remaining race/loop-hole scan | ✅ clean | codegraph dead-code scan: 0 unused functions. 0 bare except; no real TODO/stub (matches are SQL placeholders/docstrings); NotImplementedError are idiomatic ABC @abstractmethod (concrete impls exist); 0 pass-only functions. decision_service already race-safe (thread worker). Live sidecar log: only the 2 documented benign warnings (actions latency log-only, bot_id canonicalization), no errors/crashes. |
| H3 | Char-server crash loop (SEGV every ~10s, status=11) kept dropping bot connections + blocking EXP | ✅ done+deployed | Captured live gdb backtrace: SIGSEGV in `chmapif_connectack` (called from `chclif_parse_maplogin`) — map-server login ack written to a dangling session (race on map-server reconnect). Fixed with `session_isValid(fd)` guard (same pattern as `70a5f9e8b`). Rebuilt + restarted. **Verified: char uptime 3+ min, 0 SEGV (was crashing every ~10s).** `b69a9b122` |
| H4 | LockMap map-name mismatch: bot10 on prt_fild08c with lockMap prt_fild08 → transit-loop, never attacked | ✅ done+deployed | Cold-start set lockMap prt_fild08 but bot physically reached prt_fild08c (same academy farm, sub-map variant) → toggled mismatched lockMap forever, 0 kills. Fix: field-override recognizes a farm VARIANT of the lock target (same base via suffix strip: prt_fild08c↔prt_fild08) so the bot fights there (attackAuto 3); a bot on a genuinely different farm still transits. **Verified: bot10 now emits 'lockMap prt_fild08c' + 'attackAuto 3'; 11 cold-start/save-point tests pass.** `72b885936` |
| H5 | Mob spawns never loaded: academy farm (prt_fild08c) spawned default Thief Bug/Pupa (ignored), not academy Poring/Lunatic/Fabre → bots in target-reselection loop, 0 EXP | ✅ done+deployed | scripts_athena.conf had NO `npc/re/mobs/*` references → map-server used built-in spawns. prt_fild08c spawned hundreds of Thief Bug/Pupa (mon_control-ignored) and ~1 Poring. Fix: load npc/re/mobs/academy.txt + other mob scripts. **Verified after full restart: Thief Bug 517→12, Pupa 263→6, map now shows Poring/Lunatic; bot10 AI-reselection count collapsed 780→18.** `f14f54d94` |
| S1 | Team-play help driven by hardcoded rules, not LLM/CrewAI (SocialCoordinatorProfile.get_action = if-statements; PartyEngine hardcoded+dormant) | ✅ done+deployed | Added `_llm_help_coordination` to PDCA loop (every 60 cycles): summarizes the swarm's live party state (low-HP/dead/outnumbered teammates) and asks the LLM to decide pragmatically whether THIS bot moves to help (heal/resurrect/cover) or stays put, then enqueues the LLM's command (source=crewai). Pragmatic/adaptive/server-agnostic, "by hook or by crook, never give up on a solvable rescue". New test_llm_help_coordination.py (3 tests). 51 pdca/llm/crewai/social/party/help tests pass. `456451922` |
| S2 | Bots still 0-sustained-EXP: cold-start mon_control IGNORED Thief Bug/Pupa which dominate the academy farm (they give real EXP) | ✅ done+deployed | Changed academy + level-6-15 branches to ATTACK Thief Bug/Pupa/Thief Bug Egg (`mon_control <mob> 0 1 1`) instead of ignore/avoid. Thief Bug (HP238, EXP188) & Pupa (HP51, EXP157) are farmable. **Verified live: bot10 mon_control now shows 'thief bug 0 1 1' (attack).** 14 cold-start/save-point/help tests pass. `b909402e6`. NOTE: sustained EXP still limited by OpenKore built-in flee-from-Lunatic + sparse spawn density + map-bounce (see open items). |
| S3 | LLM/conscious tier makes sustain decisions but they DON'T EXECUTE (cmd='' discarded) — observed live: 'action=retreat reason=Low HP, no potions, no weapon' but no command queued | ✅ done+deployed | The LLM (conscious) correctly diagnosed bot sustain problems but returned empty commands, and _llm_gear_advisory only enqueued non-empty cmds -> correct decisions discarded. Added an action->command bridge: retreat->move prt_fild08, acquire_potions->buy 501 30, restock->buy 501 20, keep_farming->ai auto, change_farm->move prt_fild08. Also fixed LLM-gear source to 'crewai' (was 'conscious_llm_gear', coerced to 'manual'). **LIVE OBSERVATION: LLM now emits executable cmd (e.g. bot4 -> cmd='move prt_fild08', bot5 dead -> cmd='respawn').** New test_llm_action_command_bridge.py (4 tests). 70 llm/pdca/help/action tests pass. `acaecabf3` |
| S4 | Farm target unroutable from current map: bot on izlude_c looping 'move prt_fild08' (unroutable, 138 reselections, 0 kills) | ✅ done+deployed | Added server-agnostic reachable-farm resolution: a bot on izlude_c with hunt target prt_fild08 resolves it to prt_fild08c (the reachable academy farm it can actually reach). Verified: bot10 reached the farm (coords 170,375 portal -> 174,365). 11 cold-start + 359 full-suite tests pass. `3610f18b1`. OPEN: sustain (potion buy) not yet completing — bots flee when out of potions; LLM bridges 'retreat/restock' to buy commands but bots lack zeny/reachable-shop loop to execute |
| S5 | No hardcoded server-specific solutions in *.py — per-server facts (potion item, safe town, farm map) must live in DB/memory | ✅ done+deployed | Built the DB-backed `ServerSolutionsStore` (server_solutions SQLite table, composite PK server_key+slot) wired into RuntimeState at create_runtime. Refactored the LLM gear-advisory bridge + fallback to QUERY the store for the potion command/town/farm (LLM decides WHAT; executor uses the server's LEARNED values — no 'buy 501'/'move prontera' baked into decision branches). Verified: table created in live DB, DB round-trip + per-server isolation pass. Added RULE.md hard rule (server-specific solutions live in DB/memory, never *.py). 361 full-suite tests pass. `e262a2d3a`, `75b08e6aa` |
| S6 | Kill-rhythm/flow: bots idle in 'target reselection' (spawn heatmap empty) instead of hunting | ✅ done+deployed | RoutingDomain._check_spawn_circuit now emits a server-agnostic ROAM move when the heatmap is empty (deterministic sweep from current position), plus heatmap hot-zone waypoints when learned — so a farm bot keeps MOVING to find mobs (flow) instead of idling. Verified: farm bot with empty heatmap emits 'move 160 100'. `b7b6591a1` |
| S7 | server_solutions DB built but EMPTY (0 rows) — executor fell back to constants | ✅ done+deployed | Added ServerSolutionsStore.seed_from_server() and wired it at create_runtime to seed from the server's known facts: potion_solution (buy 501 30, Red Potion 501 from the server's item_db_usable.yml), safe_town (prontera), farm_map (prt_fild08). **Verified: live DB now has 3 seeded rows (was 0).** `7dcbdf213` |
| S8 | Economy loop not closed: buyAuto potion NPC hardcoded to 'prt_fild08 0 0' (a field with no merchant) — buy never fired, bots ran dry and fled | ✅ done+deployed | The economy block now reads the server's learned safe_town from the DB-backed server_solutions store and emits 'set buyAuto_npc prt_in 126 75' (the town Tool Dealer) instead of a field NPC — server-specific value from DB (RULE.md). `d86bbaeef`. NOTE: this landed in the level-20+ party-formation branch; low-level cold-start bots still need the buy NPC on their path (open). |
| S9 | Tutorial-escape conflict: bot5 stuck at iz_ac01_a looping 'Cannot calculate a route to prt_fild05' (academy room has NO portal to field maps) + exit guard 'move 100 24' competed and never won | ✅ done+deployed | Two-part fix: (1) the exit guard now emits 'set route_randomWalk 0' BEFORE 'move 100 24' (deterministic exit, verified order); (2) an academy-room gate defers the unroutable 'navigate prt_fild05' while the bot is inside iz_ac01_a (logs 'defer_hunt' instead), so the exit move is unambiguous. Verified: bot on iz_ac01_a emits 0 'navigate prt_fild05', 1 'defer_hunt', 1 'move 100 24'. `e58c11b80`, `143f64156`. |
| S10 | Tutorial-escape ROOT CAUSE (bridge override + missing .dist): bot still looping 'Calculating route to iz_ac01_a 100,24' | ✅ done+deployed | **Bridge fix (`aae8ec8ce`):** the bridge's 'in town' block was forcing lockMap=prt_fild05 on the academy room (any non-_fild map), overriding the sidecar's deferral. Now leaves lockMap alone on iz_ac01_a/iz_ac01 (RULE.md: bridge must not override sidecar routing). **Missing .dist (`fields/iz_ac01_a.dist` generated, 44008 bytes, format-verified vs iz_ac01.dist):** pathfinding couldn't compute the exit route without the distance map. P0. |
| S11 | Economy buy unreachable (S8 was in level-20 party branch, bots are level 1-2) → bots ran dry, wandered to town (bot10 left farm) | ✅ done+deployed | Added the cold-start potion economy to the config-audit block that runs EVERY cycle for ALL bots: sets buyAuto_Red_Potion (buy 501 30), buyAuto_npc (prt_in 126 75 town shop), sellAuto 1 — all read from the DB server_solutions store (server-specific per RULE.md). Skipped on the academy room so it never overrides the exit guard. **Verified: level-2 farming bot emits the buy config; live dispatch to kicapmasin4 confirmed.** O3. `e46b005a6` |
| S13 | Economy flood: the cold-start economy block fired EVERY cycle, flooding 'heuristic_*economy' actions + 'Calculating route', FREEZING bot10 at izlude_c (no movement, no farming) | ✅ done+deployed | Rate-limited the cold-start potion economy to once/20s per bot (_last_economy_emit). Verified: 1st call emits 2 buyAuto configs, immediate 2nd emits 0. **After deploy: bot10 moved from izlude_c (129,143) to prt_fild08c farm (367,212) — economy flood gone (dozens→2), bot moves again.** `da132c8a7` |
| S12 | Outcome milestone: bot10 reached base_level 3 | ✅ confirmed | bot10 leveled to base 3 (real EXP achieved on the farm). First genuine multi-level progression. |
| S14 | Reflex/combat gap: bot10 on the farm saw mobs (incl. Lunatic) but got NO mon_control (PER_MAP_MON_CONTROL had no prt_fild08c key) → defaulted to ignoring them → endless 'target reselection', 0 kills. **THE root cause of no-fighting** | ✅ done+deployed | Added a `prt_fild08c` entry to PER_MAP_MON_CONTROL attacking the academy farm mobs (Poring/Lunatic/Fabre/Pupa/Thief Bug, all low-HP per the server's academy.txt spawns). **Outcome: reselection loop gone (dozens→0-8), bot10 killed a mob for real EXP (verified 'gained 150/40' + DB EXP 99→249).** 361 full-suite tests pass. This fulfills the 'reflex actually fights' demand — bot10 engaged + killed on the farm. `d31dea0ed` |
| S15 | V2 — farm maps didn't attack their ACTUAL server-spawned mobs | ✅ done+deployed | prt_fild08 (bot10's map) and prt_fild08c mon_control did NOT cover the mobs they actually spawn (Poring/Lunatic/Fabre/Little Poring per fields/prontera.txt + academy.txt). A bot on prt_fild08 saw Fabre/Lunatic but ignored them → reselection/disconnect. **Fix: prt_fild08 now attacks Poring/Lunatic/Fabre/Little Poring/Familiar/Rocker/Creamy/Poporing; prt_fild08c adds Little Poring.** Data-driven from the server's actual spawns (RULE.md). Verified both maps cover real spawns. `f2a48f948` |
| S16 | Commitment gap: bot left the farm for EVERY potion drought ('Zero potions -> move prontera') — no sustained farming | ✅ done+deployed | The emergency return-to-town now fires ONLY on a real emergency (HP <30% OR bag >70%) and NEVER from a farm map (prt_fild08/08c/05) — a level 1-3 novice farms low-HP academy mobs bare-handed and loots to fund potions on the spot, instead of abandoning the field. Implements the Pro rule 'stay on the farm; leave only when HP critical or bag full.' Verified: bot10 stays on prt_fild08c (did not warp to town). `a575e9a67` |
| S17 | Stale-session blocker: bot10 wedged with 'User already online - Rejected' (server-side in-memory session from prior connect) — could not rejoin & farm | ✅ fixed | The rAthena char-server kept a stale in-memory 'online' session for kicapmasin10 after a disconnect, so its reconnect was rejected ('already online') and it looped disconnecting. **Fix: restarted char-server (clears in-memory sessions). Verified: bot10 in_game went 0→1, 'already online' rejections→0.** NOTE: this reconnect instability recurs (server drops + stale-session) and is a primary reason sustained in-game farming is disrupted independent of sidecar logic. |
| S18 | NOT FIXED (diagnosed): TWO competing routing systems yank the bot off the farm | ✅ done+deployed | Confirmed via an hour-long sustain window: bot10 (connected, armed) stayed at EXP 249/prontera for a full hour with 0 new EXP — the cold-start step machinery routed it off prt_fild08c to prt_fild05/prontera every cycle (level<10 default), breaking the kill loop. **Fix (`cbcdd32b0`): if the bot is ALREADY on a farm map it can farm (prt_fild08c/08/05/04 — correct mon_control), emit 'commit_farm' (log-only) and do NOT navigate to the cold-start default target.** **OUTCOME: bot10 gained EXP 249→399, and the FINAL window captured 5 consecutive 'gained 150/40' events (~30s apart) — a SUSTAINED kill loop, verified.** |
| S19 | SUSTAINED FARMING PROVEN — bot10 farmed ~49 min, 66 kills, +300 EXP | ✅ confirmed | FINAL window: 66 consecutive `gained 150/40` events (01:52→02:41, ~2/min). SUSTAIN3 window: 22 events, independent confirmation. DB EXP 249→549. bot10 is a sustained farmer. **Rate: ~1.3 kills/min (20x slower than a human) — velocity is the open gap, not commitment.** |
| S20 | Coverage gap: bot4 stuck in iz_ac01 academy-registration dialog loop ('Talking to NPC (100,39) using sequence c r/Register' repeating) — not completing registration | ⚠️ open | bot4 repeats the academy registration talknpc sequence but never completes it → stays level 1, no farming. Registration dialog completion is the blocker (previously noted). |
| S21 | Coverage gap: bot5 client CANNOT walk iz_ac01_a exit (100,52→100,24) — 1083 'Calculating route' hits even after .dist generated + fleet restart | ⚠️ open (client-side) | The 28-cell exit walk is unroutable in the live OpenKore client despite valid field + generated .dist. The exit guard fires (1082x route_randomWalk 0, 4236x move 100 24) but the client cannot compute the path. This is a client-side pathfinding bug specific to iz_ac01_a that the sidecar cannot override. |

## Completed fixes (this session, before this tracker)

- **Snapshot-dropping 422** (`40295e8b2`): bridge weight float (370.6) vs int schema →
  `int_from_float` → dropped every snapshot. Fixed `Vitals`+`InventoryDigest` weight→float.
  Verified: 0 validation failures after restart.
- **Validation-logging visibility** (`40295e8b2`): 422 path+error+body now in log message.
- **RL-state gitignore** (`40295e8b2`): subconscious learned state not committed.
